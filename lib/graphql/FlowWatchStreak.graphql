query FlowWatchStreak($channelID: ID!) {
  channel(id: $channelID) {
    self {
      watchStreakMilestone {
        watchStreakMilestone {
          id
          value
          shareStatus
        }
      }
    }
  }
}
