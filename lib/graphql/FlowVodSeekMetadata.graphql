query FlowVodSeekMetadata($videoId: ID!) {
  video(id: $videoId) {
    seekPreviewsURL
    muteInfo {
      mutedSegmentConnection {
        nodes {
          offset
          duration
        }
      }
    }
  }
}
