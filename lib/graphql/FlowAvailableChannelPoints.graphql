query FlowAvailableChannelPoints($login: String!) {
  user(login: $login) {
    id
    login
    channel {
      id
      self {
        communityPoints {
          availableClaim {
            id
          }
        }
      }
    }
  }
}
