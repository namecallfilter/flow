query FlowTopGames($first: Int, $after: Cursor, $options: GameOptions) {
  games(first: $first, after: $after, options: $options) {
    edges {
      cursor
      node {
        id
        displayName
        boxArtURL
        viewersCount
      }
    }
    pageInfo {
      hasNextPage
    }
  }
}
