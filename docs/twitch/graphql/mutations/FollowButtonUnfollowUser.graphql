#import "../fragments/FollowButtonFragment.graphql"

mutation FollowButton_UnfollowUser ($input: UnfollowUserInput!) {
  unfollowUser(input: $input) {
    follow {
      disableNotifications
      user {
        ...FollowButtonFragment
      }
    }
  }
}
