#import "../fragments/ContentDiscoveryPreferenceFragment.graphql"

mutation DiscoveryPreferenceMutation ($input: SetDiscoveryPreferenceInput!) {
  setDiscoveryPreference(input: $input) {
    error {
      code
    }
    preference {
      ...ContentDiscoveryPreferenceFragment
    }
  }
}
