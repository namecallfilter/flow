#import "../fragments/CreatorMessageFragment.graphql"

mutation CreateCreatorMessage ($input: CreateCreatorMessageInput!) {
  createCreatorMessage(input: $input) {
    message {
      ...CreatorMessageFragment
    }
    recipientCount
    error {
      code
    }
  }
}
