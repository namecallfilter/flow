mutation RefreshExtensionHelixTokenMutation(
  $extensionID: ID!
  $jwt: String!
) {
  refreshExtensionHelixToken(input: {
    extensionID: $extensionID,
    jwt: $jwt
  }) {
    token {
      jwt
    }
  }
}
