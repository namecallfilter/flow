mutation RefreshExtensionTokenMutation(
  $extensionID: ID!
  $channelID: ID!
  $jwt: String!
) {
  refreshExtensionToken(input: {
    extensionID: $extensionID,
    channelID: $channelID,
    jwt: $jwt
  }) {
    token {
      jwt
    }
  }
}
