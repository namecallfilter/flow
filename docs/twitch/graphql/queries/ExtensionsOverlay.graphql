query ExtensionsOverlay ($channelLogin: String) {
  user(login: $channelLogin) {
    id
    stream {
      id
      game {
        id
        name
      }
    }
    self {
      banStatus {
        isPermanent
        expiresInMs
      }
    }
  }
}
