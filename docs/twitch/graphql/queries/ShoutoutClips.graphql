#import "../fragments/ShoutoutClipFragment.graphql"

query ShoutoutClips ($login: String!, $criteria: UserClipsInput) {
  user(login: $login) {
    id
    clips(first: 5, criteria: $criteria) {
      edges {
        node {
          ...ShoutoutClipFragment
        }
      }
    }
  }
}
