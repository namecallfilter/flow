#import "../fragments/UseLiveGame.graphql"

query UseLiveBroadcast ($channelLogin: String!) {
  user(login: $channelLogin) {
    id
    lastBroadcast {
      id
      title
      game {
        ...UseLiveGame
      }
    }
  }
}
