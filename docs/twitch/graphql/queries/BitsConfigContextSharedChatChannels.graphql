#import "../fragments/CheermoteGroup.graphql"

query BitsConfigContext_SharedChatChannels ($ids: [ID!]) {
  channels: users(ids: $ids) {
    id
    cheer {
      id
      cheerGroups {
        ...CheermoteGroup
      }
    }
  }
}
