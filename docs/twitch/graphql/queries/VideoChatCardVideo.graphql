query VideoChatCard_Video ($videoID: ID!) {
  video(id: $videoID) {
    id
    title
    previewThumbnailURL(width: 90, height: 51)
    publishedAt
    owner {
      id
      login
      displayName
    }
  }
}
