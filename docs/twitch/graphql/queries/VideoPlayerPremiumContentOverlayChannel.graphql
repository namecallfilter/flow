query VideoPlayerPremiumContentOverlayChannel ($channel: String) {
  user(login: $channel) {
    id
    stream {
      id
      restriction {
        id
        options
        type
      }
    }
  }
}
