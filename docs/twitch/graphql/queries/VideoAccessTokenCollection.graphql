query VideoAccessToken_Collection ($id: ID!) {
  collection(id: $id) {
    id
    items(first: 1) {
      edges {
        cursor
        node {
          ... on Video {
            id
          }
        }
      }
    }
  }
}
