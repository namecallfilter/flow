query PartnerPlusPublicQuery ($channelID: ID!) {
  plusStatus(channelID: $channelID) {
    partnerPlusProgram {
      l1Threshold
      l2Threshold
      level
      canShowWidget
      subPoints {
        year
        month
        count
        updatedAt
      }
      widgetSetting
    }
  }
}
