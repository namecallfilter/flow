#import "../fragments/CreatorSentMessageFragment.graphql"

query CreatorSentMessages ($first: Int, $after: Cursor) {
  creatorSentMessages(first: $first, after: $after) {
    edges {
      cursor
      node {
        ...CreatorSentMessageFragment
      }
    }
    pageInfo {
      hasNextPage
    }
  }
}
