#import "../fragments/ExtensionInstallationSelfEdge.graphql"

query ExtensionsForChannel ($channelID: ID!) {
  user(id: $channelID) {
    id
    channel {
      id
      selfInstalledExtensions {
        ...ExtensionInstallationSelfEdge
      }
    }
  }
}
