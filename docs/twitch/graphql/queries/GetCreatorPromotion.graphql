#import "../fragments/CreatorPromotionActivationFragment.graphql"

query GetCreatorPromotion ($channelID: ID!) {
  channel(id: $channelID) {
    id
    creatorPromotion {
      currentActivation {
        ...CreatorPromotionActivationFragment
      }
    }
  }
}
