#import "../fragments/SharedChatParticipant.graphql"

query SharedChatSession ($channelID: ID!) {
  sharedChatSession(channelID: $channelID) {
    id
    host {
      id
    }
    status
    participants {
      ...SharedChatParticipant
    }
    createdAt
    updatedAt
    autoStartsAt
  }
}
