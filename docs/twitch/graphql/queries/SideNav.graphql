#import "../fragments/SideNavSection.graphql"

query SideNav ($input: SideNavInput!, $creatorAnniversariesFeature: Boolean!, $withFreeformTags: Boolean!) {
  sideNav(input: $input) {
    sections {
      edges {
        ...SideNavSection
      }
    }
  }
}
