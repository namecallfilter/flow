#import "../fragments/CoordinatorExtensionInstallationSelfEdge.graphql"

query CoordinatorExtensionsForChannel($channelID: ID!) {
  user(id: $channelID) {
    id
    channel {
      id
      selfInstalledExtensions {
        ...CoordinatorExtensionInstallationSelfEdge
      }
    }
  }
}
