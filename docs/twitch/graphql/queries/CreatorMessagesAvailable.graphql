query CreatorMessagesAvailable {
  creatorMessagesAvailable {
    hasReceived
    unreadCount
    isEligibleCreator
  }
  currentUser {
    id
    login
    displayName
    profileImageURL(width: 50)
  }
}
