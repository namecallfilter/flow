query FollowPanelOverlay ($channelLogin: String!) {
  user(login: $channelLogin) {
    id
    displayName
    login
    self {
      follower {
        node {
          id
        }
        disableNotifications
        followedAt
      }
    }
  }
}
