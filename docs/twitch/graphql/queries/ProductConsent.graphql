#import "../fragments/UserConsent.graphql"

query ProductConsent {
  currentUser {
    id
    createdAt
    productConsent {
      ... on ProductConsentError {
        code
      }
      ... on UserConsent {
        ...UserConsent
      }
    }
  }
}
