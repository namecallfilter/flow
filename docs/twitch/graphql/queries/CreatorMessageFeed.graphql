#import "../fragments/CreatorMessageFragment.graphql"

query CreatorMessageFeed ($first: Int, $after: Cursor) {
  creatorMessageFeed(first: $first, after: $after) {
    edges {
      cursor
      node {
        ...CreatorMessageFragment
      }
    }
    pageInfo {
      hasNextPage
    }
  }
}
