query Core_Services_Spade_EmoteCard_User ($channelID: ID!) {
  user(id: $channelID) {
    id
    hasPrime
    self {
      canPrimeSubscribe
      subscriptionBenefit {
        id
        tier
        purchasedWithPrime
      }
    }
  }
}
