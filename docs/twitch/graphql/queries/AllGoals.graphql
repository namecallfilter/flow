#import "../fragments/ChannelGoalFragment.graphql"

query AllGoals ($channelLogin: String!) {
  channel(name: $channelLogin) {
    id
    goals {
      edges {
        cursor
        node {
          ...ChannelGoalFragment
        }
      }
    }
  }
}
