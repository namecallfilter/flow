query CostreamingCurrentUser {
  currentUser {
    id
    stream {
      id
      costreamDetails {
        organizer {
          id
          login
          displayName
        }
      }
    }
  }
}
