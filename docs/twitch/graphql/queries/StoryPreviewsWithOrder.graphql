#import "../fragments/StoryPreviewFragment.graphql"

query StoryPreviewsWithOrder ($input: StoryPreviewsWithOrderInput!) {
  storyPreviewsWithOrder(input: $input) {
    previews {
      ...StoryPreviewFragment
    }
    hasNextPage
    isFresh
  }
}
