query CelebrationEmotes ($channelID: ID!) {
  user(id: $channelID) {
    id
    subscriptionProducts {
      id
      emoteGroups {
        id
        emotes {
          id
          setID
          token
          assetType
        }
      }
      tier
    }
  }
}
