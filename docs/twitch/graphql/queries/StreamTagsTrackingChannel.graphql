#import "../fragments/StreamTagsTrackingTags.graphql"

query StreamTagsTrackingChannel ($channel: String) {
  user(login: $channel) {
    id
    login
    stream {
      id
      ...StreamTagsTrackingTags
    }
  }
}
