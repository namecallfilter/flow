#import "../fragments/CostreamingDiscoveryDetails.graphql"

query CostreamingDiscoveryContextQuery ($channelIDs: [ID!]!) {
  users(ids: $channelIDs) {
    id
    login
    stream {
      id
      costreamDetails {
        ...CostreamingDiscoveryDetails
      }
    }
  }
}
