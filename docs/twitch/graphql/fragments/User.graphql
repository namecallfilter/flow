fragment User on User {
  id
  broadcastSettings {
    id
    title
  }
  displayName
  login
  profileImageURL(width: $imageWidth)
  primaryColorHex
  roles {
    isPartner
    isParticipatingDJ
  }
}
