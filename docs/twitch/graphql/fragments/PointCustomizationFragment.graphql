#import "./PointsImageFragment.graphql"

fragment PointCustomizationFragment on User {
  channel {
    id
    communityPointsSettings {
      name
      image {
        ...PointsImageFragment
      }
    }
  }
}
